"""
This is the test for integer_validator function.
"""

# Importing the function from the module:
    >>> BaseGeometry = __import__("7-base_geomerty").BaseGeometry

# Testing for module docstring:
    >>> m = __import__("7-base_geomerty").__doc__
    >>> len(m) > 1
    True

# Testing for class docstring:
    >>> c = __import__("7-base_geomerty").BaseGeometry.__doc__
    >>> len(c) > 1
    True

# Testing for method docstring:
	>>> mod = __import__("7-base_geomerty").BaseGeometry.integer_validator.__doc__
	>>> len(mod) > 1
	True
