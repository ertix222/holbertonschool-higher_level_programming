-- select all columns 'score' and 'name' of the table and ordered them by score
SELECT score, name FROM second_table
-- filter only those are above or equal to 10
WHERE score >= 10
ORDER BY score DESC;
