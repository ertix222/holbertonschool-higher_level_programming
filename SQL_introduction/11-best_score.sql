-- select all columns 'score' and 'name' of the table, ordered them by score and filter only those are above or equal to 10
SELECT score, name FROM second_table
WHERE score >= 10
ORDER BY score DESC;
